import React, {useContext} from "react";
import { Context } from "../store/appContext";


export const collapseHolidays = () => {

    const { store, actions } = useContext(Context);


    return(
        <div> acá va la info de las fiestas</div>
    )
};
